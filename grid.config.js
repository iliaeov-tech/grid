/* ============================================================
   Grid — подключение к базе.

   Заполните два значения из Supabase: Project Settings → API.
   Пока они пустые, доска показывает экран настройки, а ключи
   можно ввести прямо в браузере (они сохранятся только у вас).

   Про ключ anon: он не секретный и рассчитан на публикацию в
   браузере — им нельзя обойти права доступа. Доску защищает вход
   через Google и правила RLS в supabase-schema.sql.
   Ключ service_role сюда вставлять НЕЛЬЗЯ ни при каких условиях.
   ============================================================ */
window.GRID_CONFIG = {
  url:     "",   // https://xxxxxxxx.supabase.co
  anonKey: "",   // eyJhbGciOiJIUzI1NiIs...
  auth:    "google"   // "google" | "github" | "email"
};
